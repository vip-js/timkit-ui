<input 
  class="tk-input {{extClass}}" 
  value="{{value}}" 
  type="{{type}}" 
  password="{{password}}" 
  placeholder="{{placeholder}}" 
  placeholder-class="tk-input-placeholder" 
  disabled="{{disabled}}" 
  bindinput="handleInput"
  bindfocus="handleFocus"
  bindblur="handleBlur"
/>
