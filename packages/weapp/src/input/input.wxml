<input 
  class="{{className}}"
  type="{{type}}"
  value="{{value}}"
  placeholder="{{placeholder}}"
  disabled="{{disabled}}"
  maxlength="{{maxlength}}"
  focus="{{focus}}"
  confirm-type="{{confirmType}}"
  adjust-position="{{true}}"
  cursor-spacing="{{16}}"
  bindinput="onInput"
  bindfocus="onFocus"
  bindblur="onBlur"
  bindconfirm="onConfirm"
/>
