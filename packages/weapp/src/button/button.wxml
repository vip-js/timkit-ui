<view 
  class="{{className}}"
  bindtap="onTap"
  data-disabled="{{disabled}}"
  data-loading="{{loading}}"
>
  <view wx:if="{{loading}}" class="button-loading-icon">⏳</view>
  <slot></slot>
</view>
