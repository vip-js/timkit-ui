<view class="{{className}}" data-disabled="{{disabled}}">
  <view
    class="number-field-decrement"
    bindtap="onDecrementTap"
    data-disabled="{{api.isAtMin || disabled}}"
  >
    <text>−</text>
  </view>

  <input
    class="number-field-input"
    type="digit"
    value="{{api.value}}"
    disabled="{{disabled || readOnly}}"
    bindinput="onInput"
  />

  <view
    class="number-field-increment"
    bindtap="onIncrementTap"
    data-disabled="{{api.isAtMax || disabled}}"
  >
    <text>+</text>
  </view>
</view>
