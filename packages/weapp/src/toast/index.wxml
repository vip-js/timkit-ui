<view class="{{toastClass}}" role="status">
  <view class="flex-1">
    <view wx:if="{{title}}" class="{{titleClass}}">{{title}}</view>
    <view wx:if="{{description}}" class="{{descriptionClass}}">{{description}}</view>
  </view>
  <view wx:if="{{showClose}}" class="{{closeClass}}" bindtap="onCloseTap">
    <text>×</text>
  </view>
</view>

