import { cva } from 'class-variance-authority'

export const navbarVariants = cva('w-full')
export const navbarContentVariants = cva('flex h-16 items-center justify-between gap-4')
export const navbarBrandVariants = cva('flex items-center gap-2')
export const navbarNavVariants = cva('flex items-center gap-4')
export const navbarItemVariants = cva('flex items-center')
export const navbarActionsVariants = cva('flex items-center gap-2')
