import { cva } from 'class-variance-authority'

export const checkboxTreeRootVariants = cva('flex flex-col gap-1')
export const checkboxTreeNodeVariants = cva('flex flex-col ms-4')
export const checkboxTreeNodeContentVariants = cva('flex items-center gap-2 py-1')
