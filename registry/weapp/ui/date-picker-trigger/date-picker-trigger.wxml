<button 
    class="{{triggerApi.className}}"
    id="{{triggerApi.id}}"
    bindtap="handleTap"
    aria-label="{{triggerApi['aria-label']}}"
    data-state="{{triggerApi['data-state']}}"
>
    <slot />
</button>
