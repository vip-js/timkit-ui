<view 
  class="inline-flex h-10 items-center justify-center rounded-md bg-muted p-1 text-muted-foreground {{listApi.className}}" 
  id="{{listApi.id}}"
  role="tablist"
  aria-orientation="{{listApi['aria-orientation']}}"
>
  <slot />
</view>
